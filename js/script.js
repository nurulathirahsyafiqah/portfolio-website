// Toggle the navigation menu on mobile devices
function toggleMenu() {
  const nav = document.querySelector('nav');
  nav.classList.toggle('active');
}

// Logging the portfolio loaded message
console.log("Portfolio Loaded!");
